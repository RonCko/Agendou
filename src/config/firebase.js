// Firebase support removed. This file kept as a harmless stub so leftover imports don't crash the app.
// If you see this message, remove any remaining imports of `src/config/firebase.js` and
// ensure the code uses `src/config/supabase.js` instead.

console.warn('NOTICE: Firebase support has been removed. Do not use src/config/firebase.js');

module.exports = {};